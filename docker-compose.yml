version: '3.8'

services:
  dashboard:
    build: 
      context: ./web_monitor
    container_name: home_ai_dashboard
    depends_on:
      - ollama
    privileged: true # Added for testing host reboot capability
    restart: always
    ports:
      - "8050:5000" # Maps Jetson 8050 -> Container 5000
    runtime: nvidia
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
  
  ollama:
      image: ollama/ollama:latest
      container_name: ollama_jetson
      restart: always
      runtime: nvidia
      ports:
        - "11434:11434"
      volumes:
        - ~/.ollama:/root/.ollama
      environment:
        - OLLAMA_KEEP_ALIVE=-1  # Keeps the model loaded indefinitely
      deploy:
        resources:
          reservations:
            devices:
              - driver: nvidia
                count: all
                capabilities: [gpu]